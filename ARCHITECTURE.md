# 무인 단말 소프트웨어 — 설계 기록

호텔 무인 체크인 키오스크의 클라이언트 소프트웨어 전체를 다시 설계하고 구현한 기록이다.
한 대의 미니 PC 안에서 렌더러·백엔드·장치 서브프로세스가 각자 프로세스로 돌고, 현장에 사람이
가지 않고도 일부 컴포넌트만 원격으로 갈아끼울 수 있어야 한다.

**이 저장소는 공개용으로 다시 만든 것이다.** 벤더 프로토콜·업장 데이터·사내 인프라 주소는
전부 걷어냈고, 하드웨어는 같은 구조의 가상 장치로 대체했다. 남은 것은 **설계와 그 근거**다.
지워진 것이 무엇인지는 [무엇이 빠졌는가](#무엇이-빠졌는가)에 적어뒀다.

```
63,700 줄 · 테스트 115 파일 / 1,371 케이스 · 프로세스 경계 4 · 컴파일타임 구현체 선택 9곳
```

---

## 1. 제약이 설계를 정했다

이 소프트웨어의 모든 선택은 세 가지 제약에서 나왔다.

**현장에 갈 수 없다.** 단말은 전국에 흩어져 있고, 한 번 방문하는 비용이 기능 하나의 개발비를
넘는다. 그래서 "고치려면 방문해야 하는 실패"를 만들지 않는 것이 다른 모든 목표보다 앞선다.

**하드웨어는 거짓말을 한다.** 장치는 자기 상태를 비트로 보고하지만, 그 비트가 무엇을 뜻하는지는
말해주지 않는다. "이송로가 비었다"가 아직 안 나온 것인지 이미 가져간 것인지는 장치가 모른다.
그 판정은 전부 우리 몫이고, 틀리면 손님 앞에서 틀린다.

**아무도 안 볼 때 돈다.** 새벽 3시에 손님 혼자 쓰는 기계다. 조용히 실패하면 아무도 모르고,
다음 손님이 그 상태를 물려받는다.

---

## 2. 토폴로지

```
┌─ Electron main ─────────────────────────────────────────┐
│                                                          │
│   렌더러 (React 19)                                       │
│      ↕ MessagePort  ← 개발에선 Socket.IO. 같은 인터페이스   │
│   백엔드 (Node)  — 비즈니스 로직 · 상태 없음                 │
│      ↕ Named Pipe                                        │
│   장치 서브프로세스 ×N  — 각자 독립 프로세스                  │
│      ↕ Serial / FFI                                      │
│   하드웨어                                                │
└──────────────────────────────────────────────────────────┘
        ↑
   supervisor (Windows 서비스) — 앱이 죽으면 되살린다
```

프로세스를 나눈 것은 성능 때문이 아니다. **경계를 협상 불가능하게 만들기 위해서다.**
같은 프로세스 안에 두면 "잠깐만 이 객체 좀 가져다 쓰자"가 반드시 생기고, 그 순간 계약은
문서가 된다. 파이프 너머에 있으면 직렬화할 수 있는 것만 건널 수 있다.

이 베팅은 매일 정산되고 있다. 개발과 프로덕션이 서로 다른 채널·트랜스포트 구현으로 도는데
비즈니스 로직은 그 사실을 모른다. 백엔드는 같은 소스에서 두 타깃으로 빌드된다 — 자식으로
도는 것과 독립 프로세스로 도는 것.

프론트를 별도 기기로 분리하는 토폴로지는 **아직 안 했다.** 경계는 그걸 염두에 두고 그었고
트랜스포트 구현체를 하나 더 붙이는 형태가 되겠지만, 실제로 해보기 전까지는 설계 의도일 뿐이다.

동시에 **한계도 분명하다.** 경계는 이음새를 사주지만 계약의 정확성은 사주지 않는다.
장치 프로토콜을 잘못 이해한 채 경계를 잘 그어봐야, 잘 그어진 경계로 틀린 값이 오갈 뿐이다.

---

## 3. 경계를 컴파일러가 지킨다

### 3.1 구현체는 컴파일 시점에 고른다

환경마다 다른 구현이 필요한 지점이 아홉 곳 있다. 런타임 분기 대신 **alias**로 고른다.

| alias | 무엇이 갈리나 |
|---|---|
| `@channel/Channel` | Socket.IO(개발) ↔ MessagePort(프로덕션) |
| `@ipc/Router` | express-ipc ↔ Electron utilityProcess |
| `@serial/Port` | 실물 시리얼 ↔ 인프로세스 시뮬레이터 |
| `@bridge/Bridge` | 부모가 있다 ↔ 없다 |
| `@log/Sink` | stdout ↔ 파일 |
| `@storage/SecureStorage` · `@platform/Platform` · `@processManager/Manager` · `@hardwareTransport/Transport` | 호스트별 |

선언은 **세 곳에 함께** 한다 — `tsconfig`(타입) · `vite`(개발) · `esbuild`(빌드).
셋이 갈리면 타입은 A를 보고 런타임은 B를 도는 상태가 되므로, 코드 생성기가 새 패키지에
이 세 줄을 함께 주입한다.

백엔드는 타깃별 구현을 **표 하나**로 든다. 중첩 삼항으로 쓰면 타깃이 셋만 돼도 못 읽는다.

```js
const IMPL = {
  node:   { bridge: 'impl/absent.ts',  channel: 'impl/socket.ts',      /* … */ },
  bridge: { bridge: 'impl/parent.ts',  channel: 'impl/messagePort.ts', /* … */ },
}[TARGET];
```

### 3.2 위반은 빌드 실패로 드러난다

백엔드는 **어느 타깃에서도 electron을 참조하지 않는다.** 능력이 필요하면 부모에게 빌린다.
이걸 규칙으로만 두면 언젠가 깨지므로, 번들러 플러그인이 `electron` 해소를 잡아
어느 파일에서 새어 들어왔는지 짚으며 빌드를 세운다.

### 3.3 부모가 없다는 사실을 타입으로 표현한다

개발 환경에는 능력을 빌려줄 부모가 없다. 이때 **부재를 실패로 번역하면 안 된다** —
갈리는 것은 부재의 *의미*다.

| 호출 | 부모가 없으면 |
|---|---|
| 알리기만 하는 것 (`RENDERER_ALIVE`) | 받을 상대가 없다 = 할 일이 없다 → **성공** |
| 부모가 대행하는 것 (`PROCESS_SPAWN`) | 실제로 불가능 → **실패** |

한쪽으로 몰면 둘 다 사고가 난다. 전부 실패시켰더니 렌더러 생존 선언이 성공할 때까지
5초마다 재시도하며 개발 로그를 덮었다 — 하루 에러 262줄 중 231줄이 그거였다.
전부 성공시키면 일어나지 않은 교체를 일어난 것으로 보고해, 호출부가 오지 않을 응답을 기다린다.

### 3.4 닫힌 집합

장치 목록·네임스페이스·워밍 대상·아키텍처 선언은 전부 `satisfies Record<...>`로 묶여 있다.
한 군데를 늘리고 다른 데를 빠뜨리면 컴파일이 막힌다. **컴파일 에러가 곧 질문이다** —
"이 장치의 네이티브 의존은 호스트 런타임에 얹히는가?" 같은. 기본값을 자동으로 채워주면
그 질문을 아무도 안 하게 된다.

실제로 이 저장소를 만들며 장치를 들어냈을 때, 어긋난 지점 25곳이 타입 에러와 실패한 테스트로
전부 드러났다. 한 곳도 런타임까지 가지 않았다.

---

## 4. 하드웨어 불확실성을 타입과 테스트로 가둔다

### 4.1 상태 판정은 순수 함수로

장치는 상태 비트만 준다. "진행 중인가 / 끝났는가 / 실패했는가"는 우리가 정한다.
그 판정을 FSM 전이 조건으로 분리하고, 전이는 **무상태 순수 함수**로 둔다.

문제는 **단일 스냅샷으로 구분되지 않는 종착**이 있다는 것이다. 이송로가 비어 있다는
관측은 두 가지를 뜻할 수 있다 — 아직 안 나왔거나, 이미 가져갔거나. 직전 폴을 봐야 갈린다.

그래서 전이 조건을 두 종류로 나누고, **이력 의존을 타입이 강제하게** 했다.

```ts
interface SnapshotTransition<S> { needsPrev?: false; isComplete: (s: S) => boolean; /* … */ }
interface HistoryTransition<S>  { needsPrev: true;   isComplete: (s: S, prev: S | null) => boolean; /* … */ }
```

`prev`를 읽는 순간 술어가 2-인자가 되어 `SnapshotTransition`에 안 맞는다. 컴파일러가
`needsPrev: true` 선언을 요구하고, 그 선언은 **"이 전이는 이력이 필요하다"를 코드에 못 박는다.**
상태는 상태기가 소유하고 전이는 순수하게 남는다.

### 4.2 사고를 테스트로 박제한다

전이 조건과 프로토콜 파서에는 실제 장애가 테스트로 남아 있다.

**완료를 센서로만 판정하면 안 된다.** 토큰이 센서를 건드리는 순간 — 모터가 아직 도는데 —
완료가 나고, 그 위로 다음 명령이 들어가면 앞뒤가 맞물려 토큰이 두 센서에 걸친 채 멈춘다.
이 정지는 잼 플래그를 세우지 않아 **어떤 에러 술어에도 안 걸리고** 폴 예산만 조용히 소진한다.
실패로 끝나되 이유가 안 남는 종류다. (실측: 모터 회전 중 발사 3/3 잼, 정지 후 발사 9/9 성공)

**상태 바이트를 `parseInt`로 읽으면 안 된다.** 이 프로토콜은 니블을 `0x30 + n`으로 싣기 때문에
플래그가 겹치면 `: ; < = > ?`가 나온다. `parseInt("8<00", 16)`은 `<`에서 멈추고 `8`을 돌려준다 —
throw도 없이 **그럴듯한 거짓말**을 준다. 28개 테스트가 이 경계를 지킨다.

### 4.3 타임아웃은 사다리다

같은 동작에 대한 타임아웃이 장치·백엔드·프론트 세 층에 있다. 반드시 안쪽이 짧아야 한다 —
뒤집히면 정확히 가장 필요한 순간에만 못 쓰는 명령이 된다.

안쪽을 **벽시계로** 재는 이유가 있다. "폴 N회 × 간격"은 명목값이고 실제 비용은
`간격 + 왕복`이라, 폴 횟수로 잡으면 위층과 단위가 달라 사다리가 조용히 뒤집힌다.
한 번 겪었다: 명목 60초가 실제로는 70~75초였고 백엔드가 먼저 터졌다.

### 4.4 게이트는 선언으로

장치를 쓰기 전 생존 확인(헬스체크 → 실패 시 재탐지)을 메서드마다 첫 줄에 쓰던 것을
데코레이터로 걷었다.

```ts
@EnsureDevice(DEVICE_IDS.TOKEN_DISPENSER)
async dispense(count: number) { /* … */ }
```

중요한 건 **데코레이터가 없는 메서드**다. 상태 조회에는 일부러 안 붙였다 — "장치가 살아있나"를
묻는 쪽이 쓰는데 앞에서 재탐지가 돌면 답보다 판정이 먼저 나기 때문이다. **부재가 곧 의도의
표현**이고, 주석 없이도 grep으로 드러난다.

### 4.5 하드웨어 없이 돈다

`@serial/Port`를 시뮬레이터로 돌리면 실장비 없이 전 스택이 같은 경로로 돈다 —
전략 인코딩 → 프레이밍 → 파서 → FSM → 폴링 예산. **시뮬레이터를 위한 분기가 한 줄도 없다.**

시뮬레이터가 **모터 지연을 폴 횟수로 흉내내는 것**이 핵심이다. 즉시 완료로 만들면
`needsPrev` 전이도 모터 정지 확인도 한 번도 안 밟혀, 도는데 아무것도 검증하지 않는 데모가 된다.

---

## 5. 현장에 갈 수 없다 — 부분 업데이트

앱 전체가 아니라 **컴포넌트 단위로** 교체한다. 그러려면 세 가지 판정이 필요하고,
**이 셋은 서로 다른 질문**이다. 섞으면 전부 틀린다.

| 판정 | 질문 | 신호 |
|---|---|---|
| 되감기 | 새 백엔드가 **섰는가** | 포트 구독 선언 |
| 승격 | 이 조합이 **도는가** | 아래 |
| 계약 | 서로 **말이 통하는가** | 지문 대조 |

**승격 주체는 하나뿐이다.** 저장소 API가 이를 강제한다 — 일반 쓰기는 현재 포인터만 건드리고,
안정 포인터로 올리는 경로는 판정자만 가진다. 쓰는 주체가 둘이면 순서를 맞춰도 경합이 남는다.
실측으로 적용이 계약 판정을 **8ms 앞질러** 어긋난 조합을 "안전"으로 기록한 적이 있다.

**승격 근거는 "소프트웨어가 답했는가"지 "하드웨어가 멀쩡한가"가 아니다.** 봉투가 왔는지가
기준이고 내용이 성공인지는 아니다 — `연결 실패`라는 응답도 서브프로세스가 살아서 자기 코드를
돌렸다는 뜻이다. **응답이 아예 없는 것**만 소프트웨어 문제다. 이걸 구분 안 하면 장치가
물리적으로 빠진 단말에서 정상 업데이트가 영영 승격되지 않는다.

**`dom-ready`·`did-finish-load`를 "앱이 동작한다"로 쓰면 안 된다.** 이 착각으로 두 번 사고가 났다.
화면이 떴다는 것과 그 화면이 일한다는 것은 다른 사건이다.

### 계약 지문

각 경계(프론트↔백엔드, 백엔드↔장치)의 스키마를 정규화해 해시로 굽는다. 서로 다른 세대의
컴포넌트가 만나면 지문이 안 맞고, 그러면 되감는다.

판정은 **표면 단위**로 한다. 전체 합산 해시를 쓰면 장치 하나가 바뀌어도 프론트까지 갈아야
하는 것처럼 보인다. 장치는 자기 표면만 보면 된다.

지문이 재현 가능해야 하므로 스키마 라이브러리 버전을 정확히 고정한다. 이 저장소를 만들며
락파일 없이 설치했더니 마이너 버전이 올라가 유니온 멤버 정규화가 바뀌었고, 지문이 흔들렸다 —
그걸 "업그레이드 가드" 테스트가 잡았다.

---

## 6. 서버 상태와 클라이언트 상태

**서버에서 온 데이터를 클라이언트 스토어에 복사하지 않는다.** 이전 세대에서 전역 상태
소스가 셋(상태 라이브러리 + 디스크 저장소 + 브라우저 저장소)이었고, 그것이 최대 버그
원인이었다. 같은 사실이 세 곳에 있으면 반드시 갈린다.

서버 상태는 GraphQL 클라이언트 캐시에만 산다. 화면은 거기서 직접 읽는다.

**세션이 끝나면 게스트 스코프 캐시를 수거한다.** 수거를 *떠나는 쪽*이 아니라 **도착한 쪽**에서
하는 것이 요점이다 — 화면 전환 직후에는 이전 페이지의 구독이 아직 살아 있어, 그 자리에서
수거하면 언마운트 전에 불완전한 상태를 관측한다. 도착한 화면의 이펙트는 React 커밋 순서상
이전 트리의 언마운트가 끝난 뒤에 돈다.

클라이언트 상태는 **범위가 생명주기를 결정한다.** 스토어를 만들 때 레지스트리에 자동 등록되고,
세션이 끝나면 일괄 초기화된다. "이 값을 언제 지우지"를 사람이 기억하지 않는다.

---

## 7. 로그 — 파일은 하나, 쓰는 주체도 하나

```
[producer] Logger ──▶ Sink (주입) ──▶ stdout|file ──▶ 백엔드 [writer]
```

프로세스마다 자기 파일을 열면 **같은 사건의 앞뒤가 대여섯 개 파일에 흩어진다.** 하루 7개까지
갔다가 2개로 줄였다. 서브프로세스는 레코드를 만들기만 하고 어디로 나가는지 모른다.

- **시각은 만드는 쪽이 찍는다.** 받는 쪽이 찍으면 자식 로그가 자기 프로세스 안의 순서를 잃는다.
- **받는 쪽은 레벨을 다시 거르지 않는다.** 거르면 자기 레벨을 따로 두는 producer가 통째로 사라진다.
- **출처는 닫힌 집합이다.** 등록 안 된 출처는 경계에서 거절돼 조용히 사라진다.
- `utilityProcess.fork`는 `stdio` 기본값이 `inherit`이라 `stdout`이 `null`이다. 이걸 모르면
  패키징된 앱에서 자식 로그가 아무 데도 안 남는다.

**메인 프로세스 로그만 따로다.** 백엔드로 가는 전송이 끊긴 상황을 진단하는 것이 존재 이유라
그 전송에 얹을 수 없다. 의도적 분리다.

---

## 8. 반복은 생성기가 짊어진다

새 장치나 도메인을 추가할 때 손대야 하는 곳이 열 곳이 넘는다 — 타입·스키마·라우터·컨트롤러·
트랜스포트·워밍 전략·계약 레지스트리·빌드 alias·CI. 하나라도 빠지면 조용히 동작하지 않는다.

```bash
bun gen full TokenDispenser
```

명령 하나로 전 레포에 주입된다. 파일을 통째로 만드는 것이 아니라 **기존 파일의 앵커 마커 앞에
멱등하게** 끼워 넣는다. 생성기가 계약 지문도 다시 굽는다.

사람이 판단해야 하는 것은 하나만 남긴다: *이 서브프로세스에 부팅 중 말을 거는 경로가 있는가.*
없으면 원격 업데이트 승격 검증에서 빠진다. 조건부 설치 장치라 일부러 빼는 것은 괜찮지만,
**모르고 빠지는 것과 구별되어야 한다.**

---

## 9. 무엇이 빠졌는가

정직하게 적는다. 이 저장소에 **없는** 것과, 원본에 있었지만 **공개할 수 없는** 것.

| 빠진 것 | 이유 |
|---|---|
| 카드 결제 단말 3종 어댑터 | 벤더 전문·응답 코드가 계약 대상 |
| 도어락 카드 발급 | 벤더 SDK 역공학 결과 |
| 신분증 판독 장치 | 벤더 SDK · 개인정보 처리 경로 |
| 실물 장치 프로토콜 | 위와 같음 — 같은 구조의 가상 장치로 대체 |
| 업장 도메인 플로우 | 체크인·정산·객실 배정 등 |

**가상 장치 TD-200**은 실물을 대체한다. 모델명·명령 코드·상태 비트만 새로 정의했고,
그 위 계층(FSM 전이·뮤텍스 직렬화·폴링 예산·사고 기록)은 제품 코드 그대로다.
프로토콜 전문을 저장소에 공개할 수 있다는 것이 오히려 장점이다 — 벤더 것이었다면 못 적었다.

**중국어/일본어 입력기**는 실물 그대로 남았다. 오픈소스 엔진(librime / Mozc)을 FFI로 붙인
것이라 공개에 제약이 없다. 여기엔 `rime.dll` 바인딩과, 이름 있는 라이브러리 없이 직접 구현한
protobuf 와이어 코덱이 들어 있다.

---

## 10. 돌려보기

```bash
bun install
bun run dev          # 백엔드 + 프론트. 장치는 첫 요청에 lazy spawn
bun run test         # 전 패키지 — 1,371 케이스
bun gen full Demo    # 생성기 — 전 레포에 수직 슬라이스가 꽂힌다
```

시리얼 장치는 기본이 **시뮬레이터**라 하드웨어가 필요 없다.

### 입력기 엔진은 직접 받아서 넣어야 한다

중국어·일본어 조합은 네이티브 엔진을 FFI로 부른다. **그 바이너리는 이 저장소에 없다** —
받아서 아래 경로에 두면 그때부터 동작한다.

| 언어 | 엔진 | 두는 곳 | 받는 곳 |
|------|------|---------|---------|
| 중국어 간·번체 | librime | `~/.kiosk/rime/` (`rime.dll` + 스키마 `*.yaml`) | [rime/librime](https://github.com/rime/librime) 릴리스 · 스키마는 [rime/rime-luna-pinyin](https://github.com/rime/rime-luna-pinyin) |
| 일본어 | Mozc | `~/.kiosk/mozc/` (`mozc_broker.exe` · `mozc_server.exe` + 동봉 런타임 DLL) | [google/mozc](https://github.com/google/mozc) 를 직접 빌드 |

놓는 것은 `bun run ime:setup` 이 대신한다 — 단말에서 백엔드가 CDN 에서 받아 설치할 때와
같은 상수(`IME_ASSET_BUNDLES`)·같은 불변식(목적지엔 완전체 아니면 부재)을 쓴다.

> Mozc 바이너리를 **빌드해서** 넣어야 한다. 배포판 설치본에서 꺼낸 파일은 재배포 조건이
> 달라 여기에 동봉하지 않았고, 같은 이유로 내려받을 링크도 걸지 않는다.

일본어는 자산이 없어도 **이미 떠 있는 mozc 서버가 있으면 그쪽에 붙는다**(`session.ipc` →
파이프 핑). 위 경로는 그게 없을 때 쓰는 폴백이고, 키오스크에서는 이쪽이 주 경로다.

엔진이 없는 언어는 실패가 아니라 **부재**로 다뤄진다. 헬스체크는 "엔진 하나라도 살아 있는가"
(`some`)를 묻고, 없는 언어의 요청만 `ENGINE_NOT_READY` 로 갈려 후보 자리에 한 줄로 뜬다.
자산이 나중에 도착하면 다음 요청에서 스스로 붙는다 — 실패를 캐시하지 않는다.
관련 통합 테스트도 자산이 없으면 스스로 건너뛴다.

---

<sub>이 문서는 설계 판단과 그 근거를 남기는 것이 목적이다. 수치(에러 262줄 중 231줄, 8ms 경합,
3/3 잼 vs 9/9 성공, 로그 7개→2개)는 전부 실제 운영에서 측정한 값이다.</sub>
