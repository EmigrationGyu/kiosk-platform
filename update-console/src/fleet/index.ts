/**
 * 실재의 출처.
 *
 * 목이 있던 자리다 — 서버가 서기 전에 화면을 세우려고 뒀고, API 가 배포되면서 걷어냈다.
 * 남겨두면 두 출처가 공존해 어느 쪽을 보고 있는지 모르게 된다.
 */

export * from './health';
export { createFleet } from './server';
export * from './types';
