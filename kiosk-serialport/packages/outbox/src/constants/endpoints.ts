export {
  OUTBOX_ENDPOINTS as ENDPOINTS,
  OUTBOX_ERROR_CODE,
  OUTBOX_PIPE_PATH,
  type OutboxCause,
  type OutboxSerialEventMap as EndpointsMap,
  OutboxSerialSchemas as OutboxSchemas,
} from 'kiosk-types';
